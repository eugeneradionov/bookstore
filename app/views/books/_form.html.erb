<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :book_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :book_description %>
  </div>

  <div class="field">
    <%= form.label :publication_year %>
    <%= form.number_field :publication_year, id: :book_publication_year %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :book_price %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.number_field :height, id: :book_height %>
  </div>

  <div class="field">
    <%= form.label :width %>
    <%= form.number_field :width, id: :book_width %>
  </div>

  <div class="field">
    <%= form.label :depth %>
    <%= form.number_field :depth, id: :book_depth %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
